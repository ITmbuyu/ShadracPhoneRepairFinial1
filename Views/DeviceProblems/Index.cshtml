@model IEnumerable<ShadracPhoneRepairFinial1.Models.DeviceProblem>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/BlankLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostOfP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostOfP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeviceProblemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeviceProblemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeviceProblemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
