@model IEnumerable<ShadracPhoneRepairFinial1.Models.WalkInTimes>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/BlankLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WalkInTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WalkInTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WalkInTimesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WalkInTimesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WalkInTimesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
