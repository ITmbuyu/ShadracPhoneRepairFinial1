@model IEnumerable<ShadracPhoneRepairFinial1.Models.CApprovalMessages>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/BlankLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CMessages)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CMessages)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CApprovalMessagesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CApprovalMessagesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CApprovalMessagesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
